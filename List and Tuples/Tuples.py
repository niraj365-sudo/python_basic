#Items of tuples can't be changed
myTuples = (1,2,3)
print(myTuples)
print(type(myTuples))   #It lies under class tuple

'''
    myTuple=() =>Empty Tuple
    myTuple=(1,) => Tuple with only one element. , is required even there is only only one tuple. Otherwise it will be treated as int 
    myTuple=(1,2,3) =>Tuple with more than one element.
'''

